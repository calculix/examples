duns is a cfd code which can be found in www.sourceforge.net

call cgx in the build mode:

  cgx -b ../rae2822.fbd

Then type (in the cgx-graphic window, not the xterm!)

  read duns.fbl

The grid is the written.

Then combine the prepared input file templates with the block-structure:

  cat duns.hdr duns.bou duns.trl >| duns.script2

call duns:

  duns.e
  